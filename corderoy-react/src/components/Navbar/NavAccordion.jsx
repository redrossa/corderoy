import React from 'react';
import classNames from 'classnames';

export default class NavAccordion extends React.Component {
  render() {
    return (
        <div className={classNames('NavAccordion', this.props.className)}>

        </div>
    );
  }
}